"use client"

import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";

const Students = () => {
    return (
        <>
            <SiteHeader />
            <div className="h-screen w-full flex-center">
                Students page is still on devlopment.
            </div>
            <SiteFooter />
        </>
    );
};
export default Students;