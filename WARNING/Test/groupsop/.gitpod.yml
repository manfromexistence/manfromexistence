tasks:

  - name: Django
    command: sudo apt-get update && sudo apt-get install python3 && pip install -r requirements.txt && pip install channels && pip install channels_redis
    init: sudo apt-get update && sudo apt-get install python3 && pip install -r requirements.txt 
